# Supabase email authentication (optional in this version).
# The app works fully and stores recipes locally even without these.
# Find them in your Supabase project: Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
